# ⚡ Быстрая настройка MongoDB на Railway

## 🚀 За 3 шага

### Шаг 1: Добавьте MongoDB в Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект
3. Нажмите **"+ New"** → **"Database"** → **"Add MongoDB"**
4. Дождитесь создания MongoDB сервиса (обычно 1-2 минуты)

### Шаг 2: Настройте подключение

1. Откройте ваш основной сервис (бот) в Railway
2. Перейдите в **"Settings"** → **"Variables"**
3. Добавьте переменную:
   - **Key:** `DATABASE_URL`
   - **Value:** `${{MongoDB.MONGO_URL}}`
   - Нажмите **"Add Reference"** и выберите `MongoDB` → `MONGO_URL`

### Шаг 3: Перезапустите приложение

1. Railway автоматически перезапустит приложение при изменении переменных
2. Проверьте логи - должно появиться: `Database URL configured: mongodb://...`
3. При первом запуске Prisma автоматически создаст схему базы данных

## ✅ Готово!

Теперь ваша база данных находится на Railway и работает в той же инфраструктуре, что и приложение.

## 📚 Подробная инструкция

См. [RAILWAY_MONGODB_SETUP.md](./RAILWAY_MONGODB_SETUP.md) для:
- Миграции данных из MongoDB Atlas
- Решения проблем
- Настройки безопасности
- Мониторинга

## 🔄 Миграция данных (опционально)

Если у вас есть данные в MongoDB Atlas:

```bash
# 1. Экспорт из Atlas
mongodump --uri="YOUR_ATLAS_URI" --out=./backup

# 2. Импорт в Railway (через Railway CLI)
railway link
railway run mongorestore --uri="$MONGO_URL" ./backup/plazma
```

---

**💡 Совет:** Используйте Reference Variables (`${{MongoDB.MONGO_URL}}`) для автоматической синхронизации.


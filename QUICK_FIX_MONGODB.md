# ⚡ Быстрое исправление MongoDB аутентификации

## 🔍 Проблема

Ошибка: `SCRAM failure: Authentication failed`

Бот работает с mock данными, но не сохраняет данные в БД.

## ✅ Решение за 2 минуты

### Шаг 1: Проверьте строку подключения

1. Откройте [Railway Dashboard](https://railway.app)
2. Найдите **MongoDB сервис** в вашем проекте
3. Откройте **"Variables"**
4. Скопируйте значение `MONGO_URL` (полностью)

### Шаг 2: Обновите DATABASE_URL

1. Откройте **основной сервис (бот)** в Railway
2. Перейдите в **"Settings"** → **"Variables"**
3. Найдите `DATABASE_URL`
4. **Вариант А (рекомендуется):** Измените значение на `${{MongoDB.MONGO_URL}}`
   - Нажмите **"Add Reference"** → выберите `MongoDB` → `MONGO_URL`
5. **Вариант Б:** Вставьте скопированное значение `MONGO_URL` напрямую

### Шаг 3: Перезапуск

Railway автоматически перезапустит приложение. Проверьте логи - ошибок аутентификации быть не должно.

## 🔄 Если не помогло: Пересоздайте MongoDB

1. **Удалите MongoDB сервис:**
   - MongoDB сервис → "Settings" → "Delete Service"

2. **Создайте новый:**
   - "+ New" → "Database" → "Add MongoDB"

3. **Обновите DATABASE_URL:**
   - Используйте новое значение `${{MongoDB.MONGO_URL}}`

4. **Восстановите данные:**
   ```bash
   railway run npm run restore
   ```

## ✅ Проверка

После исправления в логах должно быть:
- ✅ `Database connected`
- ✅ `Database schema synchronized` (или `already up to date`)
- ❌ Нет ошибок `Authentication failed`

---

**💡 Совет:** Используйте Reference Variable (`${{MongoDB.MONGO_URL}}`) для автоматической синхронизации между сервисами.


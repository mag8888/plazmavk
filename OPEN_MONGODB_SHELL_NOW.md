# 🚀 Открыть MongoDB Shell прямо сейчас

## ✅ Railway CLI уже установлен!

Выполните эти команды в терминале:

### Шаг 1: Авторизуйтесь (если нужно)

```bash
railway login
```

Если уже авторизованы, переходите к шагу 2.

### Шаг 2: Подключитесь к проекту

```bash
railway link
```

Выберите проект `plazma-production` из списка.

### Шаг 3: Откройте MongoDB shell

```bash
railway run mongosh
```

Это откроет MongoDB shell прямо в терминале!

### Шаг 4: Настройте replica set

В MongoDB shell выполните:

```javascript
rs.initiate({
  _id: "rs0",
  members: [
    { _id: 0, host: "localhost:27017" }
  ]
})
```

Подождите 5-10 секунд, затем проверьте:

```javascript
rs.status()
```

Должно показать:
```json
{
  "set": "rs0",
  "myState": 1,
  "members": [
    {
      "_id": 0,
      "name": "localhost:27017",
      "stateStr": "PRIMARY"
    }
  ]
}
```

### Шаг 5: Выйдите из shell

```bash
exit
```

## 📝 Что дальше?

После настройки replica set:

1. Обновите `DATABASE_URL` на Railway (добавьте `replicaSet=rs0`)
2. Перезапустите сервис
3. Проверьте логи

**Подробная инструкция:** См. `SETUP_INSTRUCTIONS.md`

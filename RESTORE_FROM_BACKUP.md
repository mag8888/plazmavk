# 🔄 Восстановление базы данных из бэкапа Cloudinary

## 📋 Обзор

Скрипт `scripts/restore-from-cloudinary.js` позволяет восстановить базу данных из бэкапа, сохраненного в Cloudinary.

## 🚀 Быстрое восстановление

### Вариант 1: Автоматический поиск последнего бэкапа

```bash
npm run restore
```

Скрипт автоматически:
1. Подключится к Cloudinary
2. Найдет последний бэкап в папке `plazma-bot/backups`
3. Скачает его
4. Восстановит все данные в базу данных

### Вариант 2: Восстановление из конкретного URL

```bash
npm run restore "https://res.cloudinary.com/.../database-backup-2024-12-15.json"
```

## 🔧 Настройка

### Переменные окружения

Убедитесь, что установлены следующие переменные:

```env
# Cloudinary (для доступа к бэкапам)
CLOUDINARY_CLOUD_NAME=dt4r1tigf
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# База данных (Railway MongoDB или другая)
DATABASE_URL=mongodb://mongo:27017/plazma
# ИЛИ
MONGO_URL=mongodb://mongo:27017/plazma
```

## 📊 Что восстанавливается

Скрипт восстанавливает все данные в правильном порядке:

1. ✅ **Категории** (без зависимостей)
2. ✅ **Товары** (зависят от категорий)
3. ✅ **Пользователи** (основные данные)
4. ✅ **Элементы корзины** (зависят от пользователей и товаров)
5. ✅ **Заказы** (зависят от пользователей)
6. ✅ **Партнерские профили** (зависят от пользователей)
7. ✅ **Рефералы партнеров** (зависят от профилей)
8. ✅ **Транзакции партнеров** (зависят от профилей)
9. ✅ **История активации партнеров** (зависят от профилей)
10. ✅ **Отзывы**
11. ✅ **Аудио файлы**
12. ✅ **Контент бота**
13. ✅ **Платежи** (зависят от пользователей)
14. ✅ **Медиа файлы**
15. ✅ **История пользователей** (зависят от пользователей)

## 🚂 Использование на Railway

### Через Railway CLI

```bash
# 1. Подключитесь к проекту
railway link

# 2. Запустите восстановление
railway run npm run restore
```

### Через Railway Dashboard

1. Откройте Railway Dashboard
2. Перейдите в ваш сервис
3. Откройте раздел "Deployments"
4. Нажмите "View Logs"
5. Запустите команду через терминал (если доступен)

## ⚠️ Важные замечания

### Перед восстановлением

1. **Убедитесь, что база данных пустая или вы готовы перезаписать данные**
   - Скрипт использует `upsert`, поэтому существующие данные будут обновлены
   - Если нужно полностью очистить базу, сделайте это вручную

2. **Проверьте подключение к базе данных**
   ```bash
   # Проверка подключения
   railway run npx prisma db push --skip-generate
   ```

3. **Убедитесь, что переменные Cloudinary установлены**
   ```bash
   railway variables
   ```

### После восстановления

1. **Проверьте статистику восстановления**
   - Скрипт выведет количество восстановленных записей для каждой модели

2. **Проверьте данные в админке**
   - Откройте `/admin` и проверьте наличие данных

3. **Проверьте работу бота**
   - Убедитесь, что бот корректно работает с восстановленными данными

## 🔍 Поиск бэкапов в Cloudinary

Если нужно найти конкретный бэкап:

```bash
# Список всех бэкапов (через скрипт)
node -e "
import('cloudinary').then(({v2}) => {
  v2.config({
    cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
    api_key: process.env.CLOUDINARY_API_KEY,
    api_secret: process.env.CLOUDINARY_API_SECRET
  });
  v2.search.expression('folder:plazma-bot/backups AND resource_type:raw')
    .sort_by([{created_at: 'desc'}])
    .max_results(10)
    .execute()
    .then(r => console.log(JSON.stringify(r.resources, null, 2)));
});
"
```

## 🐛 Решение проблем

### Ошибка: "Бэкапы не найдены в Cloudinary"

**Причины:**
- Неверные переменные Cloudinary
- Бэкапы не были созданы
- Неправильная папка в Cloudinary

**Решение:**
1. Проверьте переменные `CLOUDINARY_*`
2. Убедитесь, что бэкапы существуют в папке `plazma-bot/backups`
3. Создайте новый бэкап: `npm run backup`

### Ошибка: "Failed to download"

**Причины:**
- Проблемы с сетью
- Неверный URL бэкапа
- Cloudinary недоступен

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь, что URL бэкапа правильный
3. Попробуйте скачать бэкап вручную и указать локальный путь

### Ошибка: "Database connection failed"

**Причины:**
- Неверная строка подключения
- База данных недоступна
- Неверные права доступа

**Решение:**
1. Проверьте переменную `DATABASE_URL` или `MONGO_URL`
2. Убедитесь, что база данных запущена и доступна
3. Проверьте права доступа к базе данных

### Ошибка: "Invalid backup format"

**Причины:**
- Файл бэкапа поврежден
- Неверный формат JSON
- Старая версия формата бэкапа

**Решение:**
1. Попробуйте другой бэкап
2. Проверьте целостность файла бэкапа
3. Убедитесь, что используется актуальная версия скрипта

## 📝 Примеры использования

### Восстановление последнего бэкапа

```bash
npm run restore
```

### Восстановление из конкретного URL

```bash
npm run restore "https://res.cloudinary.com/dt4r1tigf/raw/upload/v1234567890/plazma-bot/backups/database-backup-2024-12-15.json"
```

### Восстановление на Railway

```bash
railway run npm run restore
```

## ✅ Чеклист восстановления

- [ ] Переменные Cloudinary установлены
- [ ] Переменная `DATABASE_URL` или `MONGO_URL` установлена
- [ ] База данных доступна и пустая (или готовы к перезаписи)
- [ ] Бэкап существует в Cloudinary
- [ ] Скрипт восстановления выполнен успешно
- [ ] Данные проверены в админке
- [ ] Бот работает корректно

---

**💡 Совет:** Регулярно создавайте бэкапы перед важными изменениями. Автоматический бэкап настроен на ежедневное выполнение в 02:00 UTC.


# 🗄️ Настройка MongoDB на Railway

## 📋 Обзор

Railway предоставляет встроенный MongoDB через плагин. Это более надежное решение, чем MongoDB Atlas, так как база данных находится в той же инфраструктуре, что и приложение.

## 🚀 Быстрая настройка

### 1. Добавление MongoDB плагина в Railway

1. Откройте ваш проект в [Railway Dashboard](https://railway.app)
2. Нажмите кнопку **"+ New"** → **"Database"** → **"Add MongoDB"**
3. Railway автоматически создаст MongoDB инстанс и добавит переменную `MONGO_URL` или `DATABASE_URL`

### 2. Получение строки подключения

После добавления MongoDB плагина:

1. Откройте созданный MongoDB сервис в Railway
2. Перейдите в раздел **"Variables"**
3. Найдите переменную `MONGO_URL` или `DATABASE_URL`
4. Скопируйте значение (это будет строка подключения вида: `mongodb://mongo:27017/plazma`)

### 3. Настройка переменных окружения

В вашем основном сервисе (бот) убедитесь, что установлена переменная:

```env
DATABASE_URL=mongodb://mongo:27017/plazma
```

**Важно:** Railway автоматически создает переменную `MONGO_URL` для MongoDB сервиса. Если ваше приложение использует `DATABASE_URL`, вы можете:

**Вариант 1:** Использовать `MONGO_URL` напрямую
- Railway автоматически предоставляет `MONGO_URL` для MongoDB сервиса
- Обновите код, чтобы использовать `MONGO_URL` вместо `DATABASE_URL`

**Вариант 2:** Создать переменную `DATABASE_URL` в основном сервисе
- В настройках основного сервиса добавьте переменную `DATABASE_URL`
- Значение: скопируйте из MongoDB сервиса (`MONGO_URL`)

**Вариант 3:** Использовать Reference Variable (рекомендуется)
- В настройках основного сервиса добавьте переменную `DATABASE_URL`
- В качестве значения используйте: `${{MongoDB.MONGO_URL}}`
- Это создаст ссылку на переменную из MongoDB сервиса

### 4. Обновление Prisma схемы (если нужно)

Если строка подключения имеет другой формат, убедитесь, что `prisma/schema.prisma` настроен правильно:

```prisma
datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}
```

### 5. Применение миграций

После настройки подключения:

```bash
# Локально (если нужно)
npx prisma generate
npx prisma db push

# Или через Railway CLI
railway run npx prisma generate
railway run npx prisma db push
```

## 🔧 Детальная настройка

### Шаг 1: Создание MongoDB сервиса

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"+ New"** → **"Database"**
3. Выберите **"MongoDB"**
4. Railway автоматически создаст MongoDB инстанс

### Шаг 2: Настройка подключения

После создания MongoDB сервиса:

1. Railway автоматически создаст переменную `MONGO_URL` в MongoDB сервисе
2. В основном сервисе (бот) добавьте переменную `DATABASE_URL`:
   - Значение: `${{MongoDB.MONGO_URL}}` (Reference Variable)
   - Или скопируйте значение `MONGO_URL` из MongoDB сервиса

### Шаг 3: Проверка подключения

После деплоя проверьте логи:

```bash
# Через Railway Dashboard
# Откройте раздел "Deployments" → "View Logs"

# Или через Railway CLI
railway logs
```

Ожидаемые сообщения:
- ✅ `Database URL configured: mongodb://...`
- ✅ `Prisma Client generated successfully`
- ✅ `Database connected`

## 📊 Миграция данных

### Вариант 1: Восстановление из бэкапа Cloudinary (Рекомендуется) ⭐

Если у вас есть бэкапы в Cloudinary (автоматические или ручные):

```bash
# Через Railway CLI
railway link
railway run npm run restore
```

Скрипт автоматически:
1. Найдет последний бэкап в Cloudinary
2. Скачает его
3. Восстановит все данные в новую базу

**Подробная инструкция:** См. [RESTORE_FROM_BACKUP.md](./RESTORE_FROM_BACKUP.md)

### Вариант 2: Экспорт/Импорт через mongodump/mongorestore

Если нужно мигрировать напрямую из MongoDB Atlas:

```bash
# 1. Экспорт из Atlas
mongodump --uri="mongodb+srv://user:pass@cluster.mongodb.net/plazma" --out=./backup

# 2. Импорт в Railway MongoDB
# Получите строку подключения из Railway
mongorestore --uri="mongodb://mongo:27017/plazma" ./backup/plazma
```

### Вариант 3: Через Railway CLI

```bash
# 1. Подключитесь к Railway
railway link

# 2. Экспорт из Atlas
mongodump --uri="YOUR_ATLAS_URI" --out=./backup

# 3. Импорт в Railway
railway run mongorestore --uri="$MONGO_URL" ./backup/plazma
```

### Вариант 4: Через MongoDB Compass

1. Подключитесь к MongoDB Atlas через Compass
2. Экспортируйте коллекции
3. Подключитесь к Railway MongoDB (используйте `MONGO_URL` из Railway)
4. Импортируйте коллекции

## 🔒 Безопасность

### Настройки безопасности Railway MongoDB:

1. **Автоматическая изоляция:** MongoDB доступен только внутри Railway сети
2. **Автоматические бэкапы:** Railway создает автоматические снимки
3. **Шифрование:** Все соединения шифруются

### Рекомендации:

- ✅ Используйте Reference Variables для подключения
- ✅ Не храните строки подключения в коде
- ✅ Регулярно проверяйте доступ к базе данных
- ✅ Настройте мониторинг использования ресурсов

## 📈 Мониторинг

Railway предоставляет:

1. **Метрики использования:**
   - Объем данных
   - Количество подключений
   - Использование памяти

2. **Логи:**
   - Все операции с базой данных
   - Ошибки подключения
   - Медленные запросы

3. **Автоматические алерты:**
   - Превышение лимитов
   - Проблемы с подключением

## 🔄 Обновление подключения

Если нужно обновить строку подключения:

1. Откройте MongoDB сервис в Railway
2. Перейдите в **"Settings"** → **"Variables"**
3. Проверьте значение `MONGO_URL`
4. В основном сервисе обновите `DATABASE_URL` (если используется Reference Variable, обновление произойдет автоматически)

## 🐛 Решение проблем

### Проблема: "Cannot connect to database"

**Решение:**
1. Проверьте, что MongoDB сервис запущен в Railway
2. Убедитесь, что переменная `DATABASE_URL` установлена правильно
3. Проверьте формат строки подключения

### Проблема: "Authentication failed"

**Решение:**
1. Railway MongoDB не требует аутентификации по умолчанию
2. Если используете строку с credentials, убедитесь, что они правильные
3. Попробуйте использовать `MONGO_URL` напрямую

### Проблема: "Database not found"

**Решение:**
1. Prisma автоматически создаст базу данных при первом подключении
2. Или создайте базу данных вручную через MongoDB Compass
3. Убедитесь, что имя базы данных указано в строке подключения

## ✅ Чеклист настройки

- [ ] MongoDB сервис создан в Railway
- [ ] Переменная `DATABASE_URL` настроена в основном сервисе
- [ ] Prisma Client сгенерирован (`npx prisma generate`)
- [ ] Схема базы данных применена (`npx prisma db push`)
- [ ] Приложение успешно подключилось к базе данных
- [ ] Данные мигрированы (если нужно)
- [ ] Логи показывают успешное подключение

## 🎉 Готово!

После настройки ваша база данных будет:
- ✅ Расположена в той же инфраструктуре, что и приложение
- ✅ Автоматически резервироваться
- ✅ Масштабироваться при необходимости
- ✅ Иметь низкую задержку подключения

---

**💡 Совет:** Используйте Reference Variables (`${{MongoDB.MONGO_URL}}`) для автоматической синхронизации переменных между сервисами.


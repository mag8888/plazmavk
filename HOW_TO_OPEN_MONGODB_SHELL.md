# 🔧 Как открыть MongoDB shell в Railway

## 🎯 Цель

Нужно открыть MongoDB shell для настройки replica set.

## ✅ Способ 1: Через Railway CLI (Рекомендуется)

### Шаг 1: Авторизуйтесь в Railway CLI

```bash
railway login
```

### Шаг 2: Подключитесь к проекту

```bash
railway link
```

Выберите проект `plazma-production`.

### Шаг 3: Откройте MongoDB shell

```bash
railway run mongosh
```

Это откроет MongoDB shell напрямую в терминале.

## ✅ Способ 2: Через Railway Dashboard (Веб-интерфейс)

### Шаг 1: Откройте MongoDB сервис

1. В Railway Dashboard найдите сервис **MongoDB** (зеленый листок)
2. Нажмите на него

### Шаг 2: Откройте вкладку "Database"

1. В верхней панели MongoDB сервиса найдите вкладки
2. Нажмите на вкладку **"Database"** (не "Settings"!)

### Шаг 3: Подключитесь к базе данных

В разделе "Database" вы увидите:
- **Connection String** - строка подключения
- **Connect** - кнопка для подключения
- **MongoDB Shell** - опция для открытия shell

**Вариант A: Используйте встроенный MongoDB Shell (если доступен)**
- Найдите кнопку **"Open Shell"** или **"MongoDB Shell"**
- Нажмите на нее
- Откроется веб-интерфейс MongoDB shell

**Вариант B: Используйте Connection String**
1. Скопируйте **Connection String** из раздела "Database"
2. Используйте его для подключения через локальный `mongosh`:
   ```bash
   mongosh "mongodb://mongo:password@host:port/plazma_bot?authSource=admin"
   ```

## ✅ Способ 3: Через терминал Railway (если доступен)

### Шаг 1: Откройте сервис MongoDB

1. Railway Dashboard → сервис **MongoDB**
2. Перейдите в **"Deployments"**
3. Найдите последний деплой

### Шаг 2: Откройте терминал

1. Найдите кнопку **"Terminal"** или **"Shell"**
2. Нажмите на нее
3. Откроется терминал внутри контейнера MongoDB

### Шаг 3: Запустите mongosh

В терминале выполните:

```bash
mongosh
```

## 🎯 Что делать дальше

После открытия MongoDB shell выполните:

```javascript
// Инициализируйте replica set
rs.initiate({
  _id: "rs0",
  members: [
    { _id: 0, host: "localhost:27017" }
  ]
})

// Проверьте статус (подождите 5-10 секунд)
rs.status()
```

Должно показать:
```json
{
  "set": "rs0",
  "myState": 1,
  "members": [
    {
      "_id": 0,
      "name": "localhost:27017",
      "stateStr": "PRIMARY"
    }
  ]
}
```

## 🆘 Если не получается открыть shell

### Проблема: Нет вкладки "Database"

**Решение:**
- Используйте Railway CLI: `railway run mongosh`
- Или используйте Connection String для локального подключения

### Проблема: Railway CLI не работает

**Решение:**
1. Убедитесь, что авторизованы: `railway login`
2. Убедитесь, что подключены к проекту: `railway link`
3. Попробуйте обновить CLI: `npm i -g @railway/cli@latest`

### Проблема: Нет доступа к терминалу

**Решение:**
- Используйте Railway CLI (Способ 1)
- Или используйте Connection String для локального подключения

## 📚 Дополнительная информация

После настройки replica set:
1. Обновите `DATABASE_URL` на Railway (добавьте `replicaSet=rs0`)
2. Перезапустите сервис
3. Проверьте логи

**Подробная инструкция:** См. `SETUP_INSTRUCTIONS.md`

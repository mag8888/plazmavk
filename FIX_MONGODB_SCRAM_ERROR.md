# 🔧 Исправление ошибки SCRAM failure: Authentication failed

## Проблема

В логах появляется ошибка:
```
ConnectorError(ConnectorError { user_facing_error: None, kind: AuthenticationFailed { user: "SCRAM failure: Authentication failed." }, transient: false })
```

Это означает, что учетные данные MongoDB неверны или устарели.

## Решение

### Вариант 1: Использование Railway MongoDB (Рекомендуется)

1. **Удалите старый MongoDB сервис** (если есть проблемы):
   - В Railway Dashboard найдите MongoDB сервис
   - Удалите его

2. **Создайте новый MongoDB сервис**:
   - Нажмите **"+ New"** → **"Database"** → **"Add MongoDB"**
   - Railway автоматически создаст новый MongoDB инстанс

3. **Настройте переменные окружения**:
   - В основном сервисе (бот) откройте **Settings** → **Variables**
   - Убедитесь, что есть переменная `DATABASE_URL` со значением: `${{MongoDB.MONGO_URL}}`
   - Это создаст Reference Variable, которая автоматически синхронизируется

4. **Проверьте формат URL**:
   - URL должен быть в формате: `mongodb://mongo:password@host:port/database?authSource=admin`
   - Railway автоматически добавляет `authSource=admin`

### Вариант 2: Использование MongoDB Atlas

1. **Создайте кластер в MongoDB Atlas**:
   - Перейдите на https://www.mongodb.com/cloud/atlas
   - Создайте бесплатный кластер (M0)

2. **Настройте Network Access**:
   - В Atlas Dashboard → **Network Access**
   - Добавьте IP: `0.0.0.0/0` (для тестирования) или IP Railway

3. **Создайте пользователя**:
   - В Atlas Dashboard → **Database Access**
   - Создайте пользователя с правами `readWrite`

4. **Получите connection string**:
   - В Atlas Dashboard → **Database** → **Connect** → **Connect your application**
   - Скопируйте connection string
   - Формат: `mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority`

5. **Настройте переменные на Railway**:
   - В основном сервисе добавьте `DATABASE_URL` с connection string из Atlas
   - Убедитесь, что пароль URL-encoded (специальные символы заменены на %XX)

### Вариант 3: Проверка текущих учетных данных

1. **Проверьте переменные окружения на Railway**:
   - Откройте сервис бота → **Settings** → **Variables**
   - Проверьте `DATABASE_URL` или `MONGO_URL`

2. **Проверьте формат пароля**:
   - Если пароль содержит специальные символы (`@`, `#`, `%`, `&` и т.д.), они должны быть URL-encoded
   - Например: `@` → `%40`, `#` → `%23`

3. **Проверьте имя базы данных**:
   - URL должен содержать имя базы данных: `/plazma_bot` или `/plazma`
   - Если нет, добавьте в конец URL перед `?`

## После исправления

1. **Перезапустите сервис на Railway**:
   - Откройте сервис → **Deployments** → **Redeploy**

2. **Проверьте логи**:
   - После перезапуска проверьте логи
   - Должно появиться: `Database connected` без ошибок аутентификации

3. **Проверьте работу бота**:
   - Попробуйте открыть магазин, отзывы, партнерку
   - Все должно работать без ошибок

## Дополнительная информация

- Документация Railway MongoDB: https://docs.railway.app/databases/mongodb
- Документация MongoDB Atlas: https://docs.atlas.mongodb.com/
- Формат connection string: https://www.mongodb.com/docs/manual/reference/connection-string/

## Важно

⚠️ **После изменения `DATABASE_URL` обязательно перезапустите сервис на Railway!**

⚠️ **Если используете Railway MongoDB, убедитесь, что используете Reference Variable `${{MongoDB.MONGO_URL}}`, а не копируете URL вручную.**

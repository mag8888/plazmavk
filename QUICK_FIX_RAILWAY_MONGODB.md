# ⚡ Быстрое исправление: Переключение на Railway MongoDB

## 🎯 Проблема

В логах видны ошибки:
- `SCRAM failure: Authentication failed`
- `Database unavailable, using mock user`
- `ConnectorError`

Это означает, что используется неправильная база данных или неправильные учетные данные.

## ✅ Решение за 2 минуты

### Шаг 1: Проверьте MongoDB на Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите проект `plazma-production`
3. Проверьте, есть ли сервис **MongoDB** (зеленый статус "Online")

**Если MongoDB нет:**
- Нажмите **"+ New"** → **"Database"** → **"Add MongoDB"**
- Подождите 1-2 минуты до создания

### Шаг 2: Настройте DATABASE_URL

1. Откройте сервис **plazma** (основной сервис бота)
2. Перейдите в **Settings** → **Variables**
3. Найдите переменную `DATABASE_URL`

**Вариант A (Рекомендуется):**
- Удалите старую переменную `DATABASE_URL` (если есть)
- Нажмите **"New Variable"**
- **Key:** `DATABASE_URL`
- **Value:** `${{MongoDB.MONGO_URL}}`
- Нажмите **"Add Reference"** → выберите `MongoDB` → `MONGO_URL`
- Сохраните

**Вариант B (Вручную):**
- Откройте сервис **MongoDB** → **Variables**
- Скопируйте значение `MONGO_URL`
- В сервисе **plazma** создайте/обновите `DATABASE_URL` с этим значением
- Убедитесь, что в конце есть имя базы: `/plazma_bot` или `/plazma`

### Шаг 3: Перезапустите сервис

1. Railway автоматически перезапустит при изменении переменных
2. Или вручную: **Deployments** → выберите последний деплой → **Redeploy**

### Шаг 4: Проверьте логи

После перезапуска в логах должно быть:
```
✅ Railway MongoDB detected
Database URL configured: mongodb://mongo:...
Added default database name: plazma_bot
Added authSource=admin for Railway MongoDB
Database connected
```

**НЕ должно быть:**
- ❌ `SCRAM failure`
- ❌ `Authentication failed`
- ❌ `ConnectorError`

## 🔍 Проверка формата DATABASE_URL

Правильный формат для Railway MongoDB:
```
mongodb://mongo:password@host:port/plazma_bot?authSource=admin
```

Код автоматически добавит:
- Имя базы данных (`/plazma_bot`) если его нет
- Параметр `authSource=admin` если его нет

## ⚠️ Важно

1. **Используйте Reference Variable** `${{MongoDB.MONGO_URL}}` вместо копирования URL вручную
2. **Перезапустите сервис** после изменения переменных
3. **Подождите 1-2 минуты** после перезапуска для полной инициализации

## 🆘 Если не помогло

1. **Удалите старую переменную** `DATABASE_URL` полностью
2. **Создайте новую** с Reference Variable
3. **Проверьте**, что MongoDB сервис запущен (статус "Online")
4. **Перезапустите** оба сервиса (MongoDB и plazma)

## 📊 После исправления

Проверьте работу:
- ✅ Магазин открывается без ошибок
- ✅ Отзывы загружаются
- ✅ Партнерка работает
- ✅ Нет ошибок `Database unavailable` в логах

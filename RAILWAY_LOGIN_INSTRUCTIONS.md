# 🔐 Инструкция по авторизации в Railway CLI

## Проблема

При выполнении `railway link` появляется ошибка:
```
Unauthorized. Please run `railway login` again.
```

## ✅ Решение

### Шаг 1: Авторизуйтесь в Railway CLI

Выполните в терминале:

```bash
railway login
```

Это откроет браузер для авторизации:
1. Браузер откроется автоматически
2. Войдите в свой аккаунт Railway
3. Разрешите доступ CLI к вашему аккаунту
4. После успешной авторизации вернитесь в терминал

### Шаг 2: Проверьте авторизацию

```bash
railway whoami
```

Должно показать ваш email или username.

### Шаг 3: Подключитесь к проекту

```bash
railway link
```

Выберите проект `plazma-production` из списка.

## 🆘 Если не работает

### Проблема: Браузер не открывается

**Решение:**
1. Скопируйте URL из терминала
2. Вставьте его в браузер вручную
3. Авторизуйтесь

### Проблема: "Cannot login in non-interactive mode"

**Решение:**
1. Убедитесь, что выполняете команду в интерактивном терминале
2. Не используйте команду через скрипты или автоматизацию
3. Выполните команду напрямую в терминале

### Проблема: Все еще "Unauthorized"

**Решение:**
1. Попробуйте выйти и войти снова:
   ```bash
   railway logout
   railway login
   ```

2. Проверьте, что используете правильный аккаунт Railway

3. Убедитесь, что Railway CLI обновлен:
   ```bash
   npm i -g @railway/cli@latest
   ```

## 📚 После авторизации

После успешной авторизации вы сможете:

1. **Подключиться к проекту:**
   ```bash
   railway link
   ```

2. **Открыть MongoDB shell:**
   ```bash
   railway run mongosh
   ```

3. **Выполнить команды в контексте Railway:**
   ```bash
   railway run npm run restore
   ```

## 💡 Альтернатива: Настройка через Railway Dashboard

Если Railway CLI не работает, можно настроить все через веб-интерфейс:

1. Откройте https://railway.app
2. Войдите в свой аккаунт
3. Выберите проект `plazma-production`
4. Откройте сервис **MongoDB** → **Data** → **Connect**
5. Используйте веб-интерфейс для настройки replica set

**Подробная инструкция:** См. `SETUP_INSTRUCTIONS.md`
